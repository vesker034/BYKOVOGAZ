# -*- coding: utf-8 -*-
"""Приведение блока «Обзор технологий» и согласованных абзацев к фактическому стеку проекта."""

from pathlib import Path

from docx import Document

DOC_PATH = Path(r"c:\Users\andre\Downloads\Telegram Desktop\filippovdiplom11-1.docx")


def _set_paragraph_text(paragraph, new_text: str) -> None:
    runs = paragraph.runs
    if not runs:
        paragraph.add_run(new_text)
        return
    runs[0].text = new_text
    for r in runs[1:]:
        r.text = ""


def main():
    doc = Document(str(DOC_PATH))

    # Индексы с учётом вводного абзаца о стеке после п. 1.4 (см. update_diploma_14_stack_intro.py).
    changes = {
        213: (
            "Форма обратной связи с валидацией полей на стороне клиента (JavaScript) и сервера "
            "(Django);"
        ),
        225: (
            "Защита от SQL-инъекций: обращение к данным через ORM Django и параметризованные "
            "запросы к СУБД, без конкатенации пользовательского ввода в SQL-текст;"
        ),
        227: (
            "Хранение персональных данных пользователей исключительно на российских серверах "
            "(файл базы данных SQLite или экземпляр СУБД на площадке с локацией РФ);"
        ),
        233: (
            "HTML5 (HyperText Markup Language 5) — стандартизированный язык разметки гипертекста, "
            "определяющий структуру веб-страниц. Актуальная версия HTML5 поддерживает семантические "
            "элементы, встроенное видео, геолокацию, локальное хранилище данных, что позволяет "
            "создавать структурированный и доступный код. CSS3 (Cascading Style Sheets 3) — "
            "технология каскадных таблиц стилей, отвечающая за визуальное оформление веб-страниц. "
            "CSS3 обеспечивает реализацию адаптивной вёрстки, анимаций, переходов, сеточных систем, "
            "кастомных шрифтов. Использование CSS3 позволяет отделить логику представления от "
            "содержимого страниц, что упрощает сопровождение. Итоговая разметка формируется в "
            "шаблонах Django и передаётся клиенту как готовый HTML."
        ),
        234: (
            "JavaScript (JS) — интерпретируемый язык программирования с динамической типизацией, "
            "являющийся стандартом для создания интерактивных элементов веб-страниц на стороне "
            "клиента. В проекте используется JavaScript без подключения одностраничных фреймворков "
            "(React, Vue и аналогов): реализованы переключение темы оформления, поведение "
            "навигации, проверка полей форм и клиентская логика галереи, что согласуется с "
            "архитектурой серверного рендеринга Django."
        ),
        235: (
            "Bootstrap 5 — открытый CSS-фреймворк для разработки адаптивных веб-интерфейсов. "
            "Предоставляет готовые компоненты (навигация, карточки, модальные окна), сетку и "
            "утилитарные классы; применяется в проекте для единообразной адаптивной вёрстки."
        ),
        236: (
            "Python 3 — высокоуровневый язык программирования общего назначения. В проекте серверная "
            "логика реализована с использованием веб-фреймворка Django 6.0.3: маршрутизация "
            "запросов, ORM для работы с базой данных, встроенная административная панель, система "
            "шаблонов, защита форм от CSRF, поддержка интернационализации (локали ru/en). Выбор "
            "Django обоснован скоростью разработки корпоративного сайта и наличием средств "
            "безопасности и администрирования контента."
        ),
        237: (
            "SQLite — встраиваемая реляционная СУБД в виде файла базы данных, применяемая в проекте "
            "на этапах разработки и опытной эксплуатации. Доступ к данным выполняется через ORM "
            "Django; при росте нагрузки допускается перенос на PostgreSQL с сохранением моделей и "
            "миграций."
        ),
        238: (
            "Visual Studio Code — бесплатный кроссплатформенный редактор исходного кода от Microsoft, "
            "поддерживающий подсветку синтаксиса для Python, HTML, CSS, JavaScript и шаблонов "
            "Django, работу с виртуальным окружением, встроенный терминал и интеграцию с Git."
        ),
        239: (
            "Git / GitHub — распределённая система контроля версий Git позволяет отслеживать изменения "
            "кода, работать в ветках и откатывать ошибочные правки; GitHub используется как "
            "облачное хранилище репозитория проекта и среда совместной работы."
        ),
        240: (
            "Figma — браузерный инструмент для прототипирования и проектирования пользовательских "
            "интерфейсов: макеты страниц, согласование визуальной стилистики и передача спецификаций "
            "в вёрстку. Дизайн-макет корпоративного сайта ООО «Быковогаз» выполнен в Figma."
        ),
        262: (
            "Корпоративный веб-сайт на платформе Django с встроенной административной панелью "
            "позволяет уполномоченным сотрудникам обновлять опубликованные вакансии без участия "
            "веб-студии при каждом изменении, что снижает операционные затраты. Интеграция страницы "
            "«Контакты» с виджетом Яндекс.Карт облегчает ориентирование по адресам офиса и "
            "промышленной площадки."
        ),
        271: (
            "5. Backend разработка. Для обеспечения динамического обновления контента реализована "
            "серверная часть на платформе Django (Python 3) с СУБД SQLite и доступом к данным через "
            "ORM Django."
        ),
        276: (
            "Организация просмотра галереи изображений: адаптивная сетка миниатюр и отображение "
            "снимков в полном размере средствами вёрстки и клиентских сценариев JavaScript без "
            "подключения отдельной библиотеки лайтбокса."
        ),
        75: (
            "4. Дронов В. А. Django 4. Практика создания веб-сайтов на Python. "
            "— СПб. : БХВ-Петербург, 2023. — 752 с."
        ),
    }

    for idx, text in changes.items():
        _set_paragraph_text(doc.paragraphs[idx], text)

    doc.save(str(DOC_PATH))
    print(f"Обновлён файл: {DOC_PATH}")


if __name__ == "__main__":
    main()
